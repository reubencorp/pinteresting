<%= form_for(@bark) do |f| %>
  <% if @bark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bark.errors.count, "error") %> prohibited this bark from being saved:</h2>

      <ul>
      <% @bark.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :full_name %><br>
    <%= f.text_field :full_name %>
  </div>
  <div class="field">
    <%= f.label :dog %><br>
    <%= f.text_field :dog %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
